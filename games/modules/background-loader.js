import { ImageLoader } from "./graphics/image-loader.js";
import { Background } from "./graphics/background.js";

class BackgroundLoader {
    static LoadBackgrounds(path, backgroundData) {
        const backgrounds = {};

        return backgrounds;
    }

    static LoadBackgroundAtlas(path, backgroundData) {
        const backgrounds = {};

        return backgrounds;
    }
}

export { BackgroundLoader };
